1. Replace composer.json with a valid version

On your machine (or in the GitHub web editor), open composer.json and replace everything with this:

{
  "name": "laravel/laravel",
  "description": "The Laravel Framework.",
  "keywords": [
    "framework",
    "laravel"
  ],
  "license": "MIT",
  "type": "project",
  "require": {
    "php": "^8.3",
    "laravel/framework": "^11.0",
    "laravel/ui": "^4.6",
    "stancl/tenancy": "^3.9",
    "stripe/stripe-php": "^17.4",
    "laravel/tinker": "^2.10",
    "spatie/laravel-permission": "^6.0",
    "laravel/sanctum": "^4.0"
  },
  "require-dev": {
    "fakerphp/faker": "^1.21",
    "mockery/mockery": "^1.5",
    "phpunit/phpunit": "^10.5",
    "symfony/css-selector": "^6.2",
    "symfony/dom-crawler": "^6.2",
    "laravel/breeze": "^2.3"
  },
  "autoload": {
    "classmap": [
      "database"
    ],
    "psr-4": {
      "App\\": "app/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "tests/"
    },
    "classmap": [
      "tests/TestCase.php"
    ]
  },
  "scripts": {
    "post-root-package-install": [
      "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
    ],
    "post-install-cmd": [
      "php scripts/offline-optimize.php"
    ],
    "post-update-cmd": [
      "php scripts/offline-optimize.php"
    ],
    "pre-install-cmd": [
      "php scripts/composer-token.php"
    ],
    "pre-update-cmd": [
      "php scripts/composer-token.php"
    ]
  },
  "config": {
    "preferred-install": "dist",
    "allow-plugins": {
      "kylekatarnls/update-helper": true
    }
  }
}

Notes:

This is valid JSON:

Starts with { and ends with }

No comments

No trailing commas


I’ve removed all automatic php artisan key:generate calls from the scripts.


Commit and push this change.


---

2. Make sure scripts/offline-optimize.php doesn’t call key:generate

Double-check that file contains the “safe” version (no key:generate anywhere). If you haven’t already, update it as we discussed earlier.

If you want, you can quickly verify in the repo:

grep -R "key:generate" -n

Only results should be old history or docs, not code that runs in CI.


---

3. Push and re-run CI

After fixing composer.json and confirming offline-optimize.php is safe:

1. Commit changes


2. Push to main


3. Let GitHub Actions run again

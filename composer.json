"scripts": {
  "post-root-package-install": [
    "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
  ],

  // Either remove this block entirely, OR leave it empty.
  // It runs only on `composer create-project`, not on Codex/CI.
  "post-create-project-cmd": [
  ],

  "post-install-cmd": [
    "php scripts/offline-optimize.php"
  ],
  "post-update-cmd": [
    "php scripts/offline-optimize.php"
  ],
  "pre-install-cmd": [
    "php scripts/composer-token.php"
  ],
  "pre-update-cmd": [
    "php scripts/composer-token.php"
  ]
}

'use strict';

angular.module('myApp')
	.service('Services', ['$rootScope', '$cookies', '$cookieStore', '$http', 'config', '$q',
	function ($rootScope, $cookie, $cookieStore, $http, config, $q) {
		
	}]);

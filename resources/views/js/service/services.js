'use strict';

angular.module('myApp')
	.service('Request', ['$rootScope', '$cookies', '$cookieStore', '$http', 'config', '$q',
		function ($rootScope, $cookie, $cookieStore, $http, config, $q) {

	}]);

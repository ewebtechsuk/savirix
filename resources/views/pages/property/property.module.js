angular.module('myApp.property', ['myApp.property.addProperty', 'myApp.property.listProperty', 'myApp.property.viewProperty'])
    .config(config);

function config() {
    // module configuration can be added here
}


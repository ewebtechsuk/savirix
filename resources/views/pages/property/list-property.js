'use strict';

angular
  .module('myApp.Property', ['ngRoute'])
  .controller('PropertyCtrl', ['$scope', '$http', 'config', 'Properties', 
    function($scope, $http, config, Properties) {

    
  }]);